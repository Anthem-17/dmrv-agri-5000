# Features

This directory is reserved for future domain-oriented modules such as:

- `auth`
- `projects`
- `verification`
- `marketplace`
- `analytics`

The current prototype keeps the implementation lightweight and page-driven, but the folder is present for a scalable enterprise structure.
